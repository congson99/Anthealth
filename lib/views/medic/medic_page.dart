import 'package:flutter/cupertino.dart';

class MedicPage extends StatelessWidget {
  const MedicPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Medic Page"));
  }
}
